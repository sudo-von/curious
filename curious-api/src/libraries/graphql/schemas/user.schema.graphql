type User {
  avatar: String!
  details: UserDetails
  id: ID!
  metrics: UserMetrics
  name: String!
  username: String!
}

type Query {
  getUserById(id: ID!): User
  getUserByUsername(username: String!): User
}
